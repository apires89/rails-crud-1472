<h1>Show Page</h1>

<p><%= @restaurant.name %>-<%= @restaurant.country %>
rating:
  <% @restaurant.rating.times do %>
  ⭐️
  <% end %>
</p>

<%= link_to "edit", edit_restaurant_path(@restaurant) %>
<%= link_to "delete", restaurant_path(@restaurant),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
